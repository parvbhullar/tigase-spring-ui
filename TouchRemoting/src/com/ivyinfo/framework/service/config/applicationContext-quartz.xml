<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE beans PUBLIC "-//SPRING//DTD BEAN 2.0//EN" "http://www.springframework.org/dtd/spring-beans-2.0.dtd">

<beans>
	<!--
	CronTriggerBean允许更精确的控制任务的运行时间。如果要在每天的6：00运行任务可以按照以下方式声明一个CronTriggerBean
	0 0 6 * * ?
	jobDetail属性告诉触发器调度哪一个工作，属性cronExpression告诉触发器何时触发。一个cron表达式至少有6个由空格分隔的时间元素(最多7个)从左到右元素定义如下
	   1。秒(0-59)
	   2。分(0-59)
	   3。小时(0-23)
	   4。月份中的日期(1-31)
	   5。月份(1-12 或 JAN-DEC)
	   6。星期（1-7 或 SUN-SAT）
	   7。年份（1970-2099）
	  每一个元素都可以显示规定的一个值，一个区间（9－14），一个列表（9，12，14）或一个通配符(*)月份中的日期和星期中的日期这两个元素时互斥的一起应该通过设置一个问号(?)来表明不想设置那个字段
	对于cronReportTrigger我们设置的cronExpression为 0 0 6 * * ?可以读作任意月份任何日期的6时0分0秒执行触发器
	-->
<!--    
	 <bean id="cronReportTrigger" class="org.springframework.scheduling.quartz.CronTriggerBean">
	  	<property name="jobDetail">
	   		<ref bean="emailJob"/>
	  	</property>
	  	<property name="cronExpression">
	   		<value>* */5 * * * ?</value>
	  	</property>
	 </bean>
	 <bean class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
	  	<property name="triggers">
	   		<list>
	    		<ref bean="cronReportTrigger"/>
	   		</list>
	  	</property>
	 </bean>
-->    
        <!-- 定义调用对象和调用对象的方法 -->

	<bean id="schedulerService" class="com.ivyinfo.framework.service.quartz.SchedulerServiceImpl">
		<property name="jdbcTemplate">  
			<ref bean="jdbcTemplate" />  
		</property>
	</bean>

	<bean name="quartzScheduler" class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
		<property name="dataSource">  
			<ref bean="dataSource" />  
		</property>
		<property name="applicationContextSchedulerContextKey"  value="applicationContextKey" />
		<property name="configLocation" value="classpath:com/ivyinfo/framework/service/config/quartz.properties"/>
	</bean>

</beans>